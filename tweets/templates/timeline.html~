{% extends "layout.html" %}
{% block body %}
  <h2>Overview</h2>
    <p>

    {% if tweets %}
    <h3>Your Timeline</h3>
    <ul>
    {% for tweet in tweets %}
      <li><a href="http://twitter.com/{{ tweet.user.screen_name
}}">{{ tweet.user.screen_name }}</a>: {{ tweet.text|urlize }}
    {% endfor %}
    </ul>
	{% endif %}

{% endblock %}

